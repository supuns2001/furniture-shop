import { SettingsManagement } from "@/components/admin/settings-management";

export const metadata = { title: "Settings | Admin | Lumen" };

export default function SettingsPage() {
  return <SettingsManagement />;
}
