import { AdminDashboard } from "@/components/admin/dashboard";

export const metadata = {
  title: "Admin Dashboard | Lumen Furniture",
  description: "Manage your store.",
};

export default function AdminPage() {
  return <AdminDashboard />;
}
