import { CustomersManagement } from "@/components/admin/customers-management";

export const metadata = {
  title: "Customers | Admin | Lumen",
};

export default function CustomersPage() {
  return <CustomersManagement />;
}
